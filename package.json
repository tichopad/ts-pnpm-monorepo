{
  "name": "@tpm/monorepo",
  "private": true,
  "version": "1.0.0",
  "comments": {
    "turbo-filter-template": "Exclude the template package from pipeline runs"
  },
  "scripts": {
    "build": "turbo run build --filter=!@tpm/template",
    "lint": "turbo run lint --filter=!@tpm/template"
  },
  "devDependencies": {
    "eslint": "8.12.0",
    "prettier": "^2.6.2",
    "turbo": "^1.2.2",
    "typescript": "4.6.3"
  }
}
