{
  "name": "@tpm/rest-api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "format": "prettier --write --cache .",
    "lint": "TIMING=1 eslint --fix --cache --ext ts src",
    "build": "tsc --noEmit && node ./build.js",
    "dev": "wds --watch ./src/main.ts",
    "start": "node ./dist/main.js"
  },
  "eslintConfig": {
    "extends": "@tpm/eslint-config"
  },
  "devDependencies": {
    "@tpm/eslint-config": "workspace:*",
    "@tpm/tsconfig": "workspace:*",
    "@types/node": "18.7.13",
    "wds": "^0.13.2"
  },
  "dependencies": {
    "@tpm/utils": "workspace:*",
    "fastify": "4.5.3"
  }
}
